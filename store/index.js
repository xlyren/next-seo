import { indexInitializeStore } from './index/indexStore'
import { detailInitializeStore } from './detail/detailStore'
import { aboutInitializeStore } from './about/aboutStore'

const store = {
    indexInitializeStore,
    detailInitializeStore,
    aboutInitializeStore
};

export default store;
